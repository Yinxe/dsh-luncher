# 更新日志

DSH Starter 每个版本的用户可见变化。格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)。

> **本文件是发布说明的唯一来源。** CI 会按 `src-tauri/tauri.conf.json` 的版本号提取对应段落，
> 作为 GitHub Release 正文，并随 `latest.json` 下发到客户端「发现新版本」的说明里；
> 因此这里的每个字都会直接呈现给用户 —— 写「用户能看到的变化」，不要写 `refactor:` 之类的内部改动。
> 写作与发版要求见 [docs/RELEASING.md](docs/RELEASING.md)。

## [Unreleased]

### 新增

- **首页新增「DeepSeek 对话」入口**：一键在应用内独立窗口打开 DeepSeek 官方对话（chat.deepseek.com），与实例 Web UI 不同，这个入口**支持同时开多个窗口**——每次点击都会新开一个窗口，方便多个会话并排使用。
- **Profiles 双栏工作台：配置从「先选功能、再挑 profile」倒过来变成「先定 profile、再配一切」**：侧栏「插件管理」「模型配置」「配置文件」三个页面合并进「Profiles（实例与配置）」一页。左列还是实例列表（启动/停止/重启/复制/改名/删除全在），点任意一行的「配置」按钮，右列即打开该 profile 的配置工作台：**快捷配置**（Web 绑定/端口/来源，仅 Web profile）/ **模型** / **插件** / **配置文件**（`cordis.patch.yml`）/ **package.json** 五个 Tab，切来切去未保存的编辑不丢；窄窗口下工作台占满整页，「返回」回到列表。
- **0.1.7+ 模型配置按 profile 读写，并支持「同步到其他 profile…」**：dsh 从 0.1.7 起设置改为**每个 profile 独立**（存于该 profile 的 `cordis.patch.yml`）。绑定 ≥0.1.7 的 profile 在「模型」Tab 的保存只写它自己——`llm-pi-ai` / `agent-default-model` 两个条目由启动器整块接管，**其余条目、`!!js` 自定义行与节外注释逐字节不动**；旁边新增「同步到其他 profile…」，弹窗勾选其它 ≥0.1.7 的 profile 可批量写入同一份模型配置，旧版目标自动置灰跳过、失败逐个列明原因。若 patch 里出现两份同名条目（dsh 表单又改过一次），页面会警示「后者生效」。
- **旧版 dsh（<0.1.7）启动前自动还原全局配置**：0.1.7 迁移会把 `~/.dsh/settings.yaml` 改名成 `settings.yaml.imported`，旧版 dsh 不认识这个名字、启动后读不到任何配置。现在用绑定旧版的 profile 点「启动」时，启动器会先检查：`settings.yaml` 缺失就从 `.imported` **复制还原一份**（原改名文件保持不动），toast 明示还原到哪里；文件本来就在则什么都不做。
- **配置文件 Tab：迁移档案只读查看、旧全局与当前 patch 双文件切换**：0.1.7 迁移后被归档的旧全局配置可用「旧全局配置（imported）」弹窗**只读**查看（迁移时哪些节没被接受，去这里找）；绑定旧版的 profile 在本 Tab 仍可编辑全局 `settings.yaml`（可在两个文件间切换），并标注「将随 0.1.6 之前的版本支持一并弃用」。
- **通用终端面板（顶栏「终端」，右侧常驻栏）**：实例实时日志、插件安装/卸载/升级、dsh 版本安装、内置 Node 安装四类终端任务统一收进右侧同一个面板。上方任务列表按「实例 / 插件任务 / 系统任务」分组，每行状态点 + 耗时/进度；下方是选中任务的输出区：逐行按流着色（stderr 红色）、自动滚底（上滑后可一键跳回）、复制/导出到 `~/.dsh-starter/logs/`。宽屏点顶栏「终端」右栏滑入，与内容并排不遮挡；启动实例或开始任何安装时自动展开并聚焦该任务；小窗口（<1280px）自动切换为侧边抽屉（覆盖式，不挤占内容宽度）。插件任务运行中可就地取消、失败可重试、被构建脚本拦住的可「允许构建脚本并重试」；dsh 版本安装可在面板里取消；「清理」一次收走全部已结束任务。原先分散的四处输出界面由它取代：「实例终端」抽屉、插件页内置终端（现只留「N 个插件任务进行中… 在终端中查看」状态条）、版本页的安装日志卡（现只留进度 + 取消 + 跳转）与 Node 安装的滚动日志行（现只留进度条）。
- **配置工作台记住上次使用的 Tab**：点开任意 profile 的「配置」时，默认停在你上次离开的 Tab（快捷配置 / 模型 / 插件 / 配置文件 / package.json）——在 web 上切到「插件」后，再打开 web-try 的配置也直接是「插件」页；从没有切换记录时显示第一个 Tab。

### 变更

- **启动方式与打开方式下沉到各 profile 卡片，可独立控制**：Profiles 左列每张卡片底部新增「启动方式」（子进程 / 独立进程）与「打开方式」（独立窗口 / 系统默认浏览器，仅 Web 类型显示）两枚小开关，各自独立生效——可以让 web 跑独立进程、其它 profile 保持子进程；从没动过开关的 profile 仍跟随全局默认。开关不再显示小字说明，鼠标悬浮即有完整解释。运行中的 profile 切换「启动方式」会弹出重启确认框：「保存并重启」立即生效并重启该实例，「取消」则配置不生效、保持原样；停止状态随意切换、不打扰。同时左列卡片布局修复了内容溢出（运行状态文本不再一字一行挤爆），并加了 `min-w-0` 约束。
- **profile「配置」入口更醒目 + 「终端」常驻头部**：卡片上的「配置」按钮从行尾图标改为紧跟名称的**图标+文字**按钮，与启动/停止等操作明显区分；Profiles 左列顶部与顶栏都有「终端」按钮，一键开合右侧通用终端面板（见「新增」），不用再从「运行状态」弹窗里找。

- **侧栏与命令面板随工作台收敛**：侧栏删去「插件管理」「模型配置」「配置文件」三项，「Profile 实例」更名「Profiles（实例与配置）」；命令面板删掉对应三条导航、新增「打开 {profile} 工作台」命令；各页文案里指向旧三页的说法全部改指工作台的对应 Tab。
- **自研插件适配提醒（待办）**：0.1.7 起插件设置只有声明 `.volatile()` 的字段支持热更新，其余需重启实例生效；本仓库随附的自研 `dshp-*` 插件（独立仓库）需要补声明，另行安排。
- **换版本启动 profile 的提示改为精简警告，并新增「复制 profile 试用启动」**：原来那页长篇风险说明（升级/降级徽标、逐条后果、建议）收敛成一句——「dsh 启动版本与上次不一致，可能因插件或配置不兼容导致启动失败」，并把出路直接做成三个按钮：**取消**、**复制 profile 试用启动**（打开复制框并预填 `<原名>-try`，复制成功后**直接启动这份副本**，跑不起来原实例也不受影响）、**同意风险，强制启动**（行为与原来「已知风险，继续启动」一致，成功启动后记为新的绑定版本）。
- **app 日志不再被「进程枚举 …ms ok=true」刷屏**：后台进程枚举是每几秒一次的常驻轮询，正常结果不再写 DEBUG 日志；只保留首轮一条 INFO 与异常/超时的 WARN，「系统日志」页里关键路径留痕不再被复读行顶出屏幕。

### 修复

- **macOS 上最小化（或隐藏到托盘）后点 Dock 图标即可唤回主窗口**：以前窗口收起后再点应用图标没有任何反应，只能从菜单栏状态栏的「打开主界面」唤出 —— 系统递给应用的「重新打开」请求没有被处理。现在点 Dock 图标会把主窗口取消最小化、带回前台并聚焦。
- **降级后 dsh Web 界面打不开（空白/渲染失败）**：高版本 dsh 的界面会往浏览器本地存储写入低版本读不懂的数据，切回低版本后页面一启动就崩溃。现在以「独立窗口」方式打开实例 Web UI 时，会在页面自身脚本运行前自动清除 `dsh.*` 的本地存储项，低版本拿到的是干净数据。**注意**：这也会顺带清掉 dsh 界面自己存在这些键里的登录态/偏好，需要重新登录一次。「系统默认浏览器」方式打开不受影响（启动器无法触达外部浏览器的存储）。
- **独立窗口打开 dsh 界面不再白屏刺眼**：以前窗口先亮出来、页面还在加载，中间是一大块刺眼白屏。现在窗口按当前主题（深色/浅色）预铺底色并隐藏创建，首屏加载完成后才显示出来；加载超过 5 秒仍会亮出窗口（显示实际内容或错误），不会「点了没反应」。
- **复制 profile 后副本一启动就报「cannot resolve profile bundle」**：复制会跳过 `node_modules`（可重建产物），但 dsh 并不会在首次启动时自动重装依赖，于是副本里声明的插件全部解析失败、进程直接退出。现在复制流程会在拷贝完成后自动执行官方 `dsh plugin --profile <副本名> install` 装好依赖（按钮显示「复制并安装依赖…」，视网络情况可能需要几十秒）；**装依赖失败则整个副本回滚删除**并给可操作提示（检查网络/镜像后重试），不再留下启动必炸的半成品。此前复制出来的旧副本若启动报此错，删除后重新复制即可。

## [0.3.0] - 2026-09-22

### 新增

- **侧栏「项目仓库」卡片新增 QQ 用户群入口**：卡片底部多出一枚「QQ 群 1067778060」按钮，点一下即复制群号，去 QQ 搜群号加群反馈问题、交流插件；侧栏折叠成图标栏时也有对应的图标按钮（悬停显示群号）。
- **侧栏新增「系统日志」页：应用内直接排查问题，不用再去翻日志文件**。9 类系统日志（app / instance / install / runtime / plugin / profile / network / ui / panic）按分类一页浏览：分类标签实时显示文件大小，尾部内容自动滚到最新，支持按级别（DEBUG/INFO/WARN/ERROR）与关键词过滤、2 秒自动刷新跟随输出、一键复制本屏。**日志可在页内一键清除**：「清除此分类」「全部清除」二次确认后删除当前与滚动出的日志文件并回收磁盘，日志系统下次写入自动重建（建议先「生成诊断包」再清）。**日志级别在页内下拉即选即生效、无需重启**（保存到设置；高级用户仍可用环境变量 `DSH_STARTER_LOG` 强制覆盖，此时页面会标出「环境变量锁定」）。原先只藏在设置抽屉里的「打开日志目录」「生成诊断包」两个按钮也挪到了这一页；「生成诊断包」现在直接在弹窗里展示全文（顶部**一键复制全文**、打开所在目录），报 bug 时不必再去文件夹里找文件。设置抽屉对应位置留下指向新页的说明。
- **全局命令面板（⌘K / Ctrl+K）**：侧栏新增「命令面板」入口按钮，或随时按 ⌘K（Windows/Linux 为 Ctrl+K）呼出居中搜索面板——输入关键词即可在**导航**（首页、版本与安装、Profile 实例、插件管理、模型配置、配置文件、凭据管理、统计、系统日志 9 个页面）、**操作**（打开 DSH 主界面、按 profile 启动/重启/停止、刷新实例与版本清单、检查启动器更新、开关实例终端、打开设置）与**外观**（深色 / 浅色 / 跟随系统）之间快速跳转执行，↑↓ 选择、Enter 执行、Esc 关闭，不用再去各页面上找按钮。
- **凭据管理防外部覆盖**：凭据页读取文件后，如果 `.credentials.yaml` 被运行中的 dsh 或其它程序改过（或删除），此时点「保存」会**检测到不符并拒绝写入**，提示先「还原」载入最新内容再重新编辑 —— 不再出现「后保存的人静默覆盖先写入内容」。凭据读写也移出主线程，大文件不再卡顿界面。
- **DSH Web 界面支持应用内独立窗口打开，像桌面版一样没有浏览器套壳**。顶栏「打开 DSH 界面」、首页、实例列表与实例终端的「打开」现在默认在应用内新窗口中呈现实例的 Web UI：没有地址栏、标签页；一个地址只对应一个窗口（再点是把已有窗口聚焦，不会越开越多），多个实例同时运行可以各开一个窗口并排使用。Profile 页新增「打开方式」单选（独立窗口 / 系统默认浏览器），随时可切回浏览器打开，全局生效。
- **配置文件编辑：语法错误一律拦在保存之前**。「配置文件」「插件管理」「Profile 配置」三处 YAML 编辑器现在共用同一套校验：写坏的 YAML 不仅在编辑器里行内标红，编辑器下方还会列出「第 X 行 第 Y 列」的具体错因，**保存按钮直接禁用**（悬停提示原因），改对了才会亮起并给出「语法校验通过，可保存」的确认；Tab 缩进这类 serde_yaml 必拒的写法也会被当场标出。保存快捷键 Ctrl/Cmd+S 顺手可用。后端写盘前的权威校验同样升级：报错带上出错行列，不用再从头数哪一行写坏了。
- **侧栏新增「统计」页：本机 dsh 的 Token 用量与在线时长一目了然**。读取本机会话日志（只读，不碰 dsh 任何配置）聚合出：全历史总览（总/输入/输出/缓存 Token、请求数、活跃天数、连续使用打卡、峰值日、最大单请求）、近 7/30/90/365 天按日模型堆叠趋势（鼠标悬浮即出当日合计与各模型明细）、今日 24 小时分布、Token 活动热力图（绿色阶、默认 6 个月可切 12 个月、自适应宽度不溢出、低用量日也清晰可见，悬浮出当日总消耗/输入输出/缓存读写/最忙模型明细）、模型用量分布卡（环形图 + 全模型响应式网格，悬浮看输入/输出/缓存构成）。口径与社区插件 token-meter 完全一致（含 fork/resume 会话去重），首次全量扫描后按文件指纹增量缓存（存在 `~/.dsh-starter/session-stats-cache.json`，可随时删除重建），之后秒开。「在线时长」标签按事件时间间隔判定在线：相邻事件间隔不超过阈值即算同一段在线，「空闲阈值」卡里 1/5/15/30/60 分钟五档并排显示各档累计与相对当前档的增量（推荐 15 分，切档秒出不用重扫），并给出「时间范围」（近 14/30/90 天/全部）只作用于每日图表；今日在线、累计在线、活跃日均、对话进行中、模型生成、工具执行、引擎合计逐项标注「精确 / 估算 / 下界」口径，底部「口径与准确性」表逐条解释每个数字怎么算、能信到什么程度；时长一律按小时显示。「分享」按钮把全历史总览、在线时长、模型用量分布按响应式版式（随窗口宽度自动降列）生成 2× 高分辨率 PNG 报表，一键复制到剪贴板或下载，直接发图。
- **侧栏新增「首页」（菜单第一项，打开应用即停留在此）**：一张卡片搞定 DSH Web 的一键启动/停止/重启/打开界面，实时显示运行状态与访问地址，窄窗口下按钮自动换行不叠字；dsh 还没初始化时该页直接给出初始化引导。
- **「一键安装 Node」现在会核对官方 SHA-256**：内置 Node 的归档下载完不再只看文件头 —— 程序边下载边计算 SHA-256，和 Node.js 官方发布的哈希逐字节比对，不一致就删掉这半份文件并告诉你「换镜像站重试」。镜像站被劫持、网关塞给你一份「长得像安装包」的文件，从这一刻起都会被拦下。
- **Profile 记住自己的启动版本，换版本启动前先让你确认风险**。每个 profile 现在会记录上一次真正把它跑起来的 dsh 版本（Profile 实例页与首页在未运行的行上显示「上次 dsh x.y.z」）；如果你切换了「当前版本」，再用一个不同的版本去启动它 —— 升级或降级都一样 —— 不再直接拉起，而是先弹出风险确认框，列明「上次成功启动 / 本次将启动」与方向（升级 / 降级），并说清插件不兼容、配置格式对不上可能让这个 profile 起不来，降级还可能遇到新版本写过的配置旧版本读不了。取消则不启动、记录不变；点「已知风险，继续启动」即启动，并把它记为新的绑定版本，同样的版本组合之后不再重复提示。从没启动过的 profile 首次启动不打扰。启动器拉起的实例活过 15 秒才会被记为「这个版本跑得起来」，所以一启动就崩掉的版本不会把提示静默掉。记录保存在 `~/.dsh-starter/profile-versions.json`，随 profile 改名 / 删除同步维护。

### 变更

- **新装用户的默认启动方式改为「独立进程」（后台常驻）**：现在点「启动」拉起的 dsh 不再随启动器退出而结束 —— 关闭或退出启动器后 DSH 继续运行，重开启动器自动扫描识别，「实例终端」里可看它的日志尾部（日志在 `~/.dsh-starter/instance-logs`）。Profile 页的「启动方式」仍可选回「子进程」（日志实时回传实例终端、退出启动器即结束所有 dsh），已保存过启动方式的老用户不受影响。
- **关键路径全部补上日志留痕：出问题能定位到「哪一步卡住」**。此前分类日志只覆盖一部分链路，现在设置读写与凭据保存（只记键名、绝不记值）、profile 配置与模型配置写盘、dsh 安装成功/取消清理/卸载与 profile 绑定版本变更、应用内更新下载验签、终端启动、插件清单修复，一直到每一次 registry / GitHub API / 模型列表请求 —— 含网络通道连续失败后的临时停用与恢复、API 额度耗尽 —— 都会落进「系统日志」页对应的分类文件。下次遇到「装不上 / 保存没反应 / 更新日志打不开」，生成诊断包就能看清失败发生在哪一步、哪条通道、什么错误。
- **README 与下载页的界面预览扩充为三张**：新增「Profile 实例 + Web 界面独立窗口」与「统计页（Token 热力图与模型分布）」两张深色主题截图；下载页「界面长这样」区块改为三张图依次展示，特性卡片同步换成「版本与安装 / Profile 实例 / 统计」。
- **托盘菜单精简为「打开主界面 / 退出」两项**：不再列出各 profile 的运行状态（● 运行中 / ○ 未运行），右键菜单更短更干净；实例状态仍可在主界面的 Profile 页查看。
- **凭据页交互全面提舒**：切到别的页面再回来，未保存的凭据编辑不再丢失（以前整页重置）；「还原」在有未保存修改时先弹出确认并列明改动数（新增 x · 修改 y · 删除 z），不再一键静默丢草稿；Ctrl/Cmd+S 直接保存；保存按钮与顶栏徽标实时显示「N 处修改」；凭据较多（≥8 条）时列表头部出现按名称/注释的即时过滤框；顶部大段规则说明收进一行可展开的折叠条，不再常驻占屏；空值凭据标出橙色「空值」徽标；双击行即开详情；添加/编辑弹窗里值超过 64KB 当场提示并禁止提交；中文输入法回车选词不再误触发提交。
- **标题栏不再重复品牌**：窗口最上方那条标签栏去掉了 logo，只留居中显示的「DSH Starter」——过去它和侧栏头部的 logo + 产品名上下叠着，两个图标两张名字。
- **侧栏「运行状态」卡片改为按钮 + 弹出详情**：常驻的「运行状态 / 目录与源」大卡片收成一个「运行状态」按钮（带运行中实例数角标），点击后在侧栏旁边弹出 Popover 显示全部内容（状态灯、实例终端入口、路径复制、托盘提示）；侧栏折叠为图标栏时，四个状态灯同样可点击弹出详情，侧栏底部更清爽。
- **dsh 更新日志改为长期磁盘缓存，正常打开不再消耗 GitHub 额度**：以前这份缓存只活在进程里（10 分钟），重启启动器后第一次打开更新日志又要打一次 api.github.com（匿名一共 60 次/小时）。现在拉到的 Release 会存进 `~/.dsh-starter/dsh-releases.json` 并**长期复用** —— 重开应用、反复查看、断网或额度耗尽都能照常翻日志，一次请求都不发。官方发了新版本想看说明时，点更新日志右上角的「刷新」强制拉一次即可（这是唯一会打 API 的入口）。
- **视觉升级：黑灰鲸鱼标**：Logo 不再用 harness 专属的品牌蓝，换成「墨黑→石墨银」渐变（侧栏、标题栏、托盘与安装包图标一并重生成）。界面上的蓝色元素（Web 徽章、外部实例圆点、RC 渠道条、任务转圈）分别改为石板灰与青绿；卡片新增投影层次、选中文本染主色，亮色页底渐变略微加深，暗色主题保持原中性灰不变。
- **打包产物直链安装只接受 https**：填 `http://…/pkg.tgz` 这类明文直链过去会原样交给安装命令，压缩包在传输途中可以被中间人换成任何内容且无从察觉。现在安装前直接拒绝，并提示换成 https 链接。

### 修复

- **Linux 上「分享 → 复制图片 / 下载 PNG」不再报 `SecurityError: The operation is insecure`**：打包后的应用里页面运行在 `tauri://` 自定义协议上，WebKitGTK 会把导出用的 `blob:` 临时链接判为跨源、污染画布，编码 PNG 那一步直接抛安全错误（开发模式是普通 http 源所以碰不到，「以前能用」就是这么来的）。现在画布被拒时自动改用同源的数据内联链接重新光栅化，导出/复制恢复可用；内联样式里的外部字体 `url()` 也一并剥掉（字体在导出图里本就不生效、文字一直走系统回退，零视觉损失），消除另一处跨源污染来源。
- **版本确认放行后如果没起来，绑定不再被记成新版本**：在风险确认框点「继续启动」后，若因端口被占等原因启动失败，profile 的绑定版本保持旧值 —— 下次启动仍会提醒；原先确认即落库，起不来反而把这道闸门静默吞掉。绑定版本改在实例确实拉起成功后写入。
- **Profile 页实例列表不再因绑定版本读取失败而整体停更**：该读取失败时降级为空清单继续展示，3 秒轮询不会连累实例状态一起卡死。
- **系统日志页三处体验修正**：快速切换分类时旧分类的响应不再落到新分类页签下；上翻阅历史时自动刷新不再每 2 秒把视线拽回底部；超长日志只渲染最近 800 条并给出提示，低配机器不再整屏卡顿。清除日志部分分类失败时，报错里也会说明其余分类已释放的字节数。
- **Web UI 独立窗口支持 IPv6 地址**：`http://[::1]:8080` 这类地址过去会因窗口名含非法字符而打不开；现在 http 与 https 同 host 端口也各自独立成窗，聚焦旧窗口不再错开成另一个协议。
- **各配置页连按 Ctrl/Cmd+S 或狂点保存不再并发写盘**：全局配置、插件、Profile 补丁与凭据保存都加了在途守卫；凭据页在任一弹窗打开时按 Ctrl+S 不再误保存底下的列表。
- **诊断包弹窗长日志不再顶穿标题**：日志很长时弹窗会被撑得比屏幕还高，正文滚到最上面时会盖住标题和「复制全文」按钮。现在弹窗最高只占屏幕 85%，正文在自己的区域里滚动，标题与操作按钮始终可见。
- **凭据保存不再因注释里的制表符/回车报「内部错误」**：注释里混进控制字符（如从别处粘贴）时，过去会生成非法 YAML 被写盘前校验拦下，用户只看到一句看不懂的「内部错误」。现在这些字符在写入前就被压成空格，注释始终是一行干净文本；凭据名称的长度限制也统一为按字符数计（含中文的名称不再前端放行、保存被拒）。
- **配置编辑器的「还原」不再在最需要它的时候被禁用**：过去有未保存修改时还原按钮反而灰着（`disabled={busy || !dirty}`），而把文件写坏之后最想做的恰恰是丢弃改动、读回磁盘版本。现在有错随时可还原，未保存改动也能一键丢弃。
- **desktop profile 被认错成 Web**：dsh 内置的 `desktop` profile 里同时带着 `@deepseek-ai/dsh-web-app`（桌面外壳内嵌 Web 界面），过去只看 bundles 会把它标成 `Web` 标签。现在先认 package.json 的 `name`：`@deepseek-ai/dsh-desktop-runtime` ⇒ `Desktop`，标签回到正确的桌面形态（桌面启动方式仍将在后续版本支持）。
- **侧栏底部的版本号张冠李戴**：`@deepseek-ai/dsh` 那行过去显示的是**启动器自己的版本**（如 v0.2.2），而不是当前安装的 dsh 版本 —— 让人误以为 dsh 也停在 0.2.2。现在启动器版本并入标题「DSH Starter v0.2.2」，`@deepseek-ai/dsh` 那行改显示真正在用的 dsh 版本，没装时显示「未安装」。
- **Windows 上「文件被占用」的安装失败不再被误报成下载超时**：杀毒软件、编辑器或还在运行的 dsh 攥住 `node_modules` 时，pnpm 报的是 EPERM/EBUSY —— 过去它被套进「下载超时」的模板：提示你去担心网络、还自动挂上一个 600 秒的超时参数重试一次（当然照样失败）。现在会直说：先停掉这个实例、关闭占用该目录的程序再重试。
- **凭据文件的备份不再是「裸奔的明文 token」**：每次保存凭据前，启动器会把旧的 `credentials.yaml` 备份成同目录下的 `.starter-bak` —— 过去这份备份按系统默认权限落成 0644，**同机器上的任何用户都能直接读走你全套明文凭据**（主文件自己是 0600，备份却忘了收紧）。现在备份与凭据主文件权限一致（0600），且主文件自创建起就是 0600，不再有几毫秒的开放窗口。
- **「停止外部实例」不再可能因损坏的进程登记误杀整机进程**：独立进程登记表（`detached.json`）若被写坏，超范围的进程号在过去会被当成负数发给系统 —— 那是「结束本用户全部进程」的语义，桌面上一切都会立刻被杀掉。现在这类进程号会在动手前被拒绝，并给出中文提示（删除该登记文件重开启动器即可）。


## [0.2.2] - 2026-09-21

### 修复

- **「一键安装 Node」不再把合法安装包判成坏文件（`.tar.xz` 校验写错了）**：下载完内置 Node 的归档后，程序会先看文件头确认它真是个压缩包 —— 而这段校验先把开头几个字节**当成 UTF-8 文本**再比对，xz 的魔数第一个字节是 `0xFD`、不是合法 UTF-8，于是它每次都被替换成「�」，比较永远不可能命中：**哪怕镜像站给的是完完整整的官方包，也会当场报「下载到的不是 tar.xz 压缩包」，Node 一次都装不上**（Windows 的 zip 因为魔数是 ASCII 的 `PK` 才没中招）。现在改成直接比对**原始字节**，并补了两条回归用例（xz / gzip / zip / 镜像站 HTML 错误页 / 空文件）。顺带把那条报错里整屏的二进制乱码换成十六进制 + 可打印字符预览，该说的话照旧：换成别的镜像站重试、归档留在哪个路径。

## [0.2.1] - 2026-09-21

### 变更

- **0.2.0 随改名带上的那套「旧名数据迁移」代码已全部移除，改名就是全新开始**：升级不再把 `~/.dsh-launcher` 搬到 `~/.dsh-starter`、不再识别 `# dsh-launcher:` 旧标记、不再接手 `*.launcher-bak` 旧备份、也不再回落读旧主题键 —— 那一版本来就没有对外用户，留着这套兼容只会一直占维护成本。**从 0.1.x 升上来的话请按全新安装处理**：想保留已装版本与 profile，把旧的 `~/.dsh-launcher` 目录自己改名为 `~/.dsh-starter` 即可。
- **自建更新源（R2）换到与产品名一致的桶**：`dsh-luncher-release` → `dsh-starter-release`，公开基址随之更新。注意 **0.2.0 及更早版本读的是旧桶地址，不会自动发现这一版** —— 需要手动下载安装一次，或在设置里把更新源切到「GitHub 源」后再检查更新。

## [0.2.0] - 2026-09-21

> **这一版换了名字，也换了「身份证」。** 程序改名 **DSH Starter**（旧名 `DSH Launcher` 里的
> `luncher` 是个不存在的词），应用标识从 `com.deepseek.dsh-launcher` 变成 `com.deepseek.dsh-starter`。
> 系统按标识认程序，于是新旧两版会被当成**两个程序**：应用内更新会把新版正常装上，但 Windows 的
> 「应用和功能」里可能同时出现新旧两条，装好后请手动卸载旧版。
> **设置、已装版本、profile、凭据都不用重新配置** —— 首次启动会自动搬迁（见下）。

### 变更

- **改名 DSH Starter：仓库、下载页、安装包与数据目录全部统一，数据自动搬家不丢一份**：仓库变为 <https://github.com/Yinxe/dsh-starter>（旧地址访问会自动跳转），下载页变为 <https://yinxe.github.io/dsh-starter/>（**旧下载页地址不再跳转**，请更新书签），安装包文件名从 `DSH.Launcher_…` 变成 `DSH.Starter_…`，窗口标题与侧栏品牌名同步换成 Starter。启动器数据目录也从 `~/.dsh-launcher` 改名成 `~/.dsh-starter`，**首次启动时自动整体搬迁**：已装 dsh 版本、profile、设置、日志与凭据原样保留（同一磁盘上是改名而非复制，几百 MB 的 `versions/` 也是瞬间完成）。万一目录被占用或跨盘搬不动，程序会继续使用旧目录并在启动日志里说明原因 —— **宁可名字不一致，也不会让你以为数据丢了**。
- **旧备份与旧标记继续被认作「自己的东西」**：启动器覆写 `package.json` / `cordis.patch.yml` / 凭据文件前生成的那份备份，后缀从 `*.launcher-bak` 改成 `*.starter-bak`，但**老备份会被自动接手改名** —— 不会被当成「还没备份过」而重新生成一份、把你原始的干净版本挤掉；写进 profile 的 `# dsh-launcher: disable` / `web-quick` 标记也继续被识别，之前关掉的插件照样能正常开回来，不会在配置里留下一堆删不掉的块。按时间戳堆积的旧备份（`*.launcher-bak-<时间>`）与旧版一样会被自动清理。
- **主题选择不会因为改名被重置**：亮/暗色的存储键换了，但读取时会回落到旧键，你之前选好的主题照旧生效。

## [0.1.8] - 2026-09-21

### 修复

- **插件开关在全新 profile 上不再一按就报 YAML 错**：profile 模板自带的 `cordis.patch.yml` 是「一串注释 + 一行 `[]`」，而 `[]` 是 flow 写法 —— 启动器往后追加 `- id: …` 条目时，同一个文件里就出现了**两个 YAML 文档**，写前校验直接失败，报「YAML 语法错误：deserializing from YAML containing more than one document is not supported」，于是**任何插件的启用/停用都点不动**（快捷配置那条写入路径早有处理，插件启停这条漏了）。现在追加前先摘掉这行空占位（`[ ]` 这种带空格的写法同样认，插件启停与快捷配置共用同一套判定），条目被清空后再把 `[]` 放回去，文件始终保持「单文档 + 顶层数组」的形态；万一文件已经被手写坏成多文档，报错也会直接说明「删掉那行 `[]`」，不再只丢一段解析器原文。
- **插件「更新」不再永远失败（中断克隆留下的坏 HEAD）**：`git clone` 中途被打断（断网、超时被杀）会在本地克隆里留下一个坏掉的 HEAD —— `ref: refs/heads/.invalid`。这个名字不合法，git 便一边「查不到这个分支」、一边「拒绝覆盖它」，于是之后每一次更新都只报 `cannot lock ref 'HEAD': reference already exists`，点多少次都不会好，插件也再更新不了（Linux/Windows 实测都中）。现在安装与更新在 fetch/pull 之前会先检查这个哨兵 HEAD，发现就把它修回真实分支、并按 `origin` 把本地分支补出来，任务日志里写明修了什么；只有这一个形态会被动，正常仓库（包括分离 HEAD）一律不碰。
- **带代理前缀的 git 地址不再把仓库永久弄脏**：代理前缀（如 `gh.dpik.top`）是临时的，而 `origin` 是永久的 —— 一旦 `<前缀>https://github.com/...` 被写进远端配置，代理失效或换域名后这个克隆就**永久 `pull` 不动**，而且启动器的加速注入只认 `https://github.com/`，对脏地址完全不生效（手抄过一次带前缀的 clone 地址就会中招）。现在：clone 前自动把前缀剥掉（落点目录名也按原始地址算），**已存在的克隆在 fetch/pull 前会把 `origin` 修回原始地址**并在任务日志里写明改了什么；安装页遇到带前缀的地址会提示「会按原始地址克隆」。顺带说明：启动器自己 clone 出来的仓库**从来不写前缀** —— 加速是进程内的 git 配置注入（`url.<前缀>.insteadOf`），进程结束即失效，`origin` 始终是 `https://github.com/...`，所以代理挂了只会退回直连，不会把克隆卡死。
- **版本表的「更多操作」菜单点得开了**：上次改动把它做成「宽屏铺开次要操作 / 窄屏收进 ⋯ 菜单」，结果两处**同时**失灵 —— 一是宽窄屏切换的 Tailwind 类名是拼接出来的（`${WIDE}:hidden`），Tailwind 只扫源码里的完整类名、对应 CSS 压根没生成，于是「铺开的次要操作」在任何宽度下都被隐藏，只剩一个 ⋯ 按钮；二是那个 ⋯ 按钮点不开：Radix 的 `asChild` 触发器靠 Slot 给子元素挂 ref 才能拿到真实 DOM，而 shadcn 的 Button 是普通函数组件、不转发 ref（上游按 React 19 写，本项目是 React 18），控制台里报 `Function components cannot be given refs`，点下去毫无反应 —— 两者叠加等于版本表里「更新日志 / 打开安装目录 / 重装 / 卸载」这一个都点不到。现在：Button 改为 `forwardRef` —— 这也是所有 `asChild` + Button 触发器的共同根因（顶栏「更多操作与环境信息」那个 ⋯ 菜单、各处 `DialogClose` / `SheetClose` 里的关闭按钮都丢 ref、控制台都报同一句警告），宽窄屏类名改成字面量。实测容器 876px 时铺开 4 个次要操作、676px 与 396px 时只剩 ⋯ 按钮，两处都能正常弹出菜单。

### 新增

- **在线下载页（DeepSeek Harness 启动器）：不用再翻 Release 页面找包** —— <https://yinxe.github.io/dsh-starter/>。按 Windows / macOS / Linux 分行列出每个安装包（.exe / .msi / .dmg / AppImage / .deb / .rpm），每行都带**可复制的直链**与体积，并自动标出你当前系统的那个包。页面上可切换**自建源（R2）**与 **GitHub Release** 两条通道，切换后全页直链、说明与安装命令一起换（选择会记住，也支持 `?source=github` 深链）：前者是路径永久不变、永远指向最新版的固定地址，适合贴给别人或写进脚本；后者与版本绑定、永久有效，适合固定版本归档。页面上的版本号、发布日期、体积与更新说明都是**打开时现拉的**（自建源清单与 GitHub Release API 并行请求、互不依赖），所以发新版后不需要重新构建页面；两条源都不通时也不会给出死链 —— 自建源的固定键地址不依赖版本号，照样能下。
- **下载页顺带把安装这件事写全了**：三个平台各一个 Tab，含每个格式的安装命令（`chmod +x` 跑 AppImage、`sudo apt install ./xxx.deb`、`msiexec /i`、macOS 拖进「应用程序」），以及两个最常被问到的坑 —— Windows SmartScreen 的「未签名」提示、macOS 未公证的「无法验证开发者」，都给了可直接复制的处理命令；另外写清了数据目录（dsh 版本装在 `~/.dsh-starter/versions/`，不污染全局 npm）。
- **下载页把能力清单也铺开了**：除了 8 条核心机制（插件为什么必须走官方命令、加速前缀为什么不写进 origin、配置为什么只重写目标小节…），页面还按「版本与安装 / 实例与终端 / 插件管理 / 配置与凭据 / 网络与更新 / 系统集成」六个领域，把 README 里的功能逐条列全 —— 不必翻文档就知道这个启动器到底管哪些事。页面**默认亮色主题**（右上角可切暗色，选择会记住），第一眼就是干净的纸底。
- **配置文件编辑器的高度不再写死**：那个代码高亮编辑器（全局配置、`cordis.patch.yml`、profile 的 `package.json` 共用）原来固定 **420px** —— 只改两行的配置也顶着一大片空白，而几百行的配置在宽屏上又白扔掉下半屏、还要多拖一条页面滚动条。现在高度自适应：**内容多高就多高**（最低留约 8 行），上限是「它在滚动容器里上方还剩多少高度」，长配置能填满窗口并只在编辑器内部滚动，页面不会被顶长、不会出现双滚动条；窗口缩放、上方说明块换行都会即时重算。编辑器在页面深处（如插件页一长串卡片之后的 `cordis.patch.yml`）或窗口很矮时会退回一个可用的固定高度，不会把长文件压成一条缝。
- **凭据可以写注释了，注释就写在键的上方**：以前 `refs` 里的每条凭据只有名字和值，一堆 `*_API_KEY` 到底哪个是干嘛的、哪个过期了，全靠记忆（只能自己在 YAML 里手写注释，而保存一次这些注释就被抹掉）。现在**文件里键正上方那一行 `# 注释` 就是这条凭据的注释**：启动器会把它读出来、在列表里新加的一列直接显示，鼠标点一下就能就地改（回车或失焦提交、Esc 取消），留空即删掉那一行；添加与编辑弹窗里也各有一个注释字段。保存改成**逐行改写 refs 块**，不再整篇重新序列化 —— `records`、`version`、未知顶层键以及它们自己的注释（records 段的小节说明、更上面的分组注释）全部逐字节保留，值里的 `#`、`:` 与多行内容（如证书）也能安全往返。
- **从本地克隆里装子包，先确认再装**：以前在「本地克隆仓库」卡片里点子包后面的 `link`，点下去就直接开装 —— 装进的是哪个 profile、装的是哪个子包、来源是哪份克隆，全都得靠悬停提示自己拼。现在会先弹一个确认框，把这几件事一次写清：目标 profile、来源仓库（目录名 + 分支 + 提交 + 是否有本地改动）、子包路径与版本、以及真正交给 `dsh plugin` 的安装规格（`link:…`）。风险也提前说：克隆里没有 `lib/` 构建产物的子包会当场警告「dsh 加载不了，装后校验会因没有可加载入口把它当场卸掉」，并指路先给这份克隆点「更新（git pull）」（会顺带 `pnpm install` + `build`）；没有声明 `dsh.bundle` 的子包（列表里的「非插件包」）`link` 按钮直接置灰，不再点了等于白装，缺 `lib/` 的候选也会在列表里先标出来。
- **每个 dsh 版本都能点开看「改了什么」**：以前想知道某个版本带来了什么，只能自己上 GitHub 翻 Release —— 版本表里只有版本号、日期、大小。现在版本表每一行都多了一个「更新日志」按钮，卡片工具栏也有一个，点开是一个版本浏览器：左栏列出全部版本（有 Release 的带绿点、早期没建 Release 的标「无发布说明」），右栏是该版本官方 Release 正文，标题与条目按原样排版；官方发的是中英双段，对话框里可以直接切「中文 / English」（默认中文），底部还有「查看这一版的完整提交对比」直达 GitHub 的 compare 页。正文一次拉全量并缓存（10 分钟 + ETag 条件请求，命中 304 不计额度），所以前后翻版本是零等待；匿名额度被限流时会提示去设置里填 GitHub Token。
- **侧栏底部重做：环境数据收成两张卡片，主栏底栏撤掉，并新增项目仓库入口**：底部数据以前散在两处 —— 侧栏是「三行灯 + registry + 数据目录」，主栏底下还压着一条状态栏写「官方源 / 数据目录 / 托盘提示」：同一批信息两处都不全，窗口一窄那条底栏还会被逐级藏掉。现在主栏底栏整个去掉，全部并进侧栏底部：「运行状态」卡片一行一灯（Node / 当前 dsh 版本 / 运行中实例数），「目录与源」把 registry、启动器数据目录、版本目录、dsh 数据目录各排一行、标签左对齐成一列；路径自动缩成 `~/.dsh-starter/versions` 这种形式（完整值仍在悬停提示里），鼠标移到行尾可一键复制完整路径。卡片最下面新增「项目仓库」卡片：显示 `Yinxe/dsh-starter`，提供「打开仓库」与「更新日志」两个按钮，反馈问题或查更新记录不用再去翻仓库地址；侧栏收起时这排退化成一排状态灯 + 一个仓库图标按钮。
- **版本列表在窄窗口下不再换行 / 横向溢出**：装过的版本每行右侧原来铺着「更新日志 / 打开安装目录 / 重装 / 卸载」这几个次要操作，窗口一窄就折成两行、三行（行高从 49px 涨到 113px，而且每行高度还参差不齐），再窄一点整张表连横向滚动条都拖出来了（表格写死了最小宽度 600px）。现在改成按**这张卡片实际有多宽**响应（容器查询，侧栏收放也算进去）：次要操作收进行尾的「更多」菜单，主操作（安装 / 设为当前）始终留在外面；「发布日期」「大小」两列按宽度逐级收起，窄到卡片只有 456px（窗口最小宽度 480px 时的实际内容宽度）仍保留「版本 / 状态 / 操作」三列。实测 456px → 1000px：**每一行都是单行 49px、零横向溢出**。顺带去掉一个重复徽标——被标记为 latest 的版本原来并排显示「LATEST」和「latest」两个同义徽标，现在只留一个。

## [0.1.7] - 2026-09-21

### 修复

- **独立进程启动的实例，侧边「实例终端」不再空白**：以「独立进程（后台常驻）」启动时，进程表还没重算（Windows 上枚举一轮要 1 秒级、另有 8 秒缓存），启动器会把刚拉起的实例误判成「已退出的残留登记」清掉 —— 日志文件路径随之丢失，于是日志文件里明明有内容，实例终端却一直读不到，实例还会被降级标成「非内嵌」。现在登记比当前进程表更新时不再判定存活，等进程表重算过再确认；独立进程的实时日志、web 地址与「打开日志」按钮恢复正常。
- **从自建源下载的安装包恢复原始文件名**：为了「一个永久不变的下载地址」，R2 上的对象键被改成了 `windows-x64-setup.exe` 这类平台名，结果手动下载时文件名里没有版本号、也认不出是哪个包（自动更新不受影响 —— 它按文件头判断类型）。现在每个对象都带 `Content-Disposition`，下载保存名恢复为原始资产名（如 `DSH.Starter_0.1.6_x64-setup.exe`），直链依旧永久不变。
- **插件安装时能看出 clone 到底走没走 GitHub 加速**：加速是靠 git 的 `insteadOf` 在进程内部改写地址的，所以地址框和终端里显示的一直是原始 github 地址，很容易误判成「没加速」。现在安装页会直接写出**实际请求地址**（没加速时说明原因：本次关掉了 / 还没测速 / 没有支持 git 的前缀），任务日志里也会逐条列出。顺带点明一个真实边界：`git@github.com:owner/repo` 这类 SSH 地址不在改写范围内，要走加速得填 https 地址。

## [0.1.6] - 2026-09-21

### 新增

- **更新下载改走自建源（Cloudflare R2）**：更新默认从自建 CDN 下载，GitHub 保留为兜底，设置里可二选一（默认 R2）。地址写死在程序里、不提供手填 —— 填错的后果是「更新不了」且无从排查。R2 上是**与版本无关的固定地址**（`latest/windows-x64-setup.exe` 这类，可直接当「永久最新版下载链接」）：每次发布覆盖同名对象，桶里永远只有一份当前最新；清单里再带一个版本指纹，保证 CDN 缓存不会把旧包喂给客户端。实测国内直连（不走代理）拉 81MB 的 AppImage：GitHub 的 release 下载**直接连不上**（返回 000），自建源 200、约 3.3MB/s —— 自建源的价值首先是**能用**。

## [0.1.5] - 2026-09-21

### 修复

- **设置里填 `~/…` 的路径现在能用了**：启动器不经过 shell 起进程，没有任何东西会替用户展开 `~`（Windows 的 cmd 本身也不认 `~`，只有 PowerShell / git-bash 认），此前把 `~/.bun/bin/node` 填进「Node 路径」、终端路径或插件「本地 link」都会静默失败。现在这几处都会自行展开 `~`（包含 `~/` 与 `~\\`）。
- **Windows 上不再到处弹黑色控制台窗口**：启动器是 GUI 程序，此前所有后台命令（git、npm、node、pnpm、PowerShell、tar、taskkill…）都没带「不创建控制台窗口」标志，于是进插件页、检查渠道、测 GitHub 加速时会一次闪出十几个黑窗口；现在后台命令统一隐藏窗口，只有你点「在终端中启动」时才给出可见的终端窗口。
- **内嵌启动 dsh 时不再闪一下黑框**：同样原因，选择「在启动器内启动」时被拉起的 node 进程此前也会短暂弹出控制台窗口。
- **Windows 上不再出现「npm 未装」/「安装 dsh 失败（os error 193）」**：官方 Node.js for Windows 会在同一个目录里同时放 `npm`（给 git-bash 用的 POSIX 脚本）和 `npm.cmd`，此前 PATH 查找先命中那个无扩展名的脚本，直接执行就报「不是有效的 Win32 应用程序」；现在优先用 `node …/node_modules/npm/bin/npm-cli.js` 执行 npm，PATH 查找也会跳过无法直接执行的无扩展名文件（`npx` / `dsh` 等同样受影响）。
- **Windows 上安装内置 Node 更可靠**：解压改为先试系统自带 tar（能解 zip，对长路径更宽容），失败再退回 PowerShell 展开；下载后会校验字节数与文件头（镜像站异常时返回的「200 + HTML 错误页」不再被当成压缩包）；解压只解出半成品目录时不再被旧目录挡住。
- **安装失败不再是看不懂的一句话**：安装 dsh、下载内置 Node 时会把**实际执行的命令、PATH、退出码、报错原文**写进 `~/.dsh-starter/logs/`（`install.log` / `runtime.log` / `app.log`），报错里也会给出日志位置；os error 193 这类错误会直接提示可能的原因。

### 新增

- **首次使用引导（dsh 初始化）**：全新机器上 `~/.dsh` 还不存在，而它正是 profile、快捷配置、插件管理的根 —— 此前界面只显示一句「未找到任何 profile」，用户会以为「装好了却什么都用不了」。现在装完版本后版本页会提示「还差一步：初始化 dsh」，Profile 实例页给出一张引导卡（带 Node / dsh / npm 就绪状态）：一键以 `dsh web`（等价 `--profile web`）完成首次启动，dsh 由此写出数据目录、内置 `web` profile 与凭据文件；随后 profile 列表自动出现（空列表期间每 3 秒重扫，也可点「我在终端里跑过了，重新检测」），默认 profile 也会自动补上。插件页的空态同样改为指路到这一步。
- **分类日志系统**：日志按子系统分文件落在 `~/.dsh-starter/logs/` —— `app`（启动阶段/托盘/设置/环境探测/自更新）、`instance`（实例启停、发现、端口归属）、`install`（npm 命令与 PATH、退出码、stderr）、`runtime`（内置 Node 下载与解压）、`plugin`（插件任务命令与失败输出）、`profile`、`network`（registry / 渠道探测 / GitHub 加速测速）、`ui`（前端报错）、`panic`（崩溃含回溯）。每行统一为「UTC 时间 + `run=` 本次启动 + `+Nms` + 级别」，`grep ERROR install.log` 就能筛问题；默认记 INFO，需要逐次细节（实例轮询、端口归属、每条外部命令）时用环境变量 `DSH_STARTER_LOG=debug` 启动。单文件超 1MB 自动滚动。
- **设置里新增「生成诊断包」**：把环境摘要（node / npm / 内置 Node 的实际路径、已装版本、profile 列表）、设置（**凭据已脱敏**）和全部分类日志合并成一个 `diagnostics-<run>.txt`；报 bug 时发这一个文件即可，不用再逐个回答环境问题。
- **设置里新增「打开日志目录」**：一键定位到 `~/.dsh-starter/logs/`。
- **前端报错回流**：`window.onerror`、未处理的 Promise、以及展示给用户的报错 toast 都会写进 `ui.log` —— 以前这类问题只停在屏幕上，事后完全无法复盘。

## [0.1.4] - 2026-09-21

### 修复

- **插件「检查更新」不再串 profile**：检查更新还在跑的时候切换 profile，此前旧 profile 的结果晚到会盖到新 profile 的列表上，「升级」按钮可能拿着旧 profile 的规格把包装错地方；现在切换后旧结果直接丢弃。
- **内置终端切 profile 自动收回隔离视图**：展开「查看其他 profile 的任务」后切到一个没有跨 profile 任务的 profile，此前开关按钮会消失、视图卡在混合模式回不去；现在切换 profile 会自动收起。
- **Windows / macOS 启动不再白屏「未响应」**：启动时创建托盘菜单会在 **UI 线程**上枚举系统进程（Windows 上要起 PowerShell，还要为每个 PID 起 `tasklist.exe`），窗口能出现但界面一直空白、标题栏显示「未响应」，把那些 `tasklist` 结束掉才恢复；现在启动路径不做任何进程枚举，托盘先显示「正在检测实例…」，真实状态由后台线程在几百毫秒内补上。
- **不再为每个监听端口各起一个 `tasklist.exe`**：Windows 上非本用户进程（`svchost` 等系统服务）的命令行读不到，此前这些进程被整条丢弃，于是「端口占用者不在进程表里」→ 每个监听端口都回退一次逐 PID 探测，一轮十几个 `tasklist` 叠加、还会闪一屏黑色控制台窗口；现在拿不到命令行的进程也保留在进程表里，逐 PID 起子进程的探测整体移除。
- **进程枚举只由一个后台线程执行，且带超时**：「前端 3 秒轮询 + 托盘 3 秒轮询」此前会各起一轮枚举，一轮没跑完下一轮就叠上来（`tasklist` 数量 16 → 18 → 20 持续增长、不回收）；现在任何时刻只有一个枚举在跑，子进程卡死会被超时结束，而不是永久占住线程。
- **进程表读不到时不再误删独立实例**：枚举失败（PowerShell 被禁用、超时）此前被当成「进程不存在」，会清掉独立进程登记条目，界面从此再也看不到、也停不掉那个实例；现在无法判定时保守保留。
- **保存设置 / 安装 / 卸载 / 改名 / 删除 profile / 读实例日志不再卡界面**：这些操作里的实例检查同样要枚举系统进程（Windows 上 1s 级），此前跑在主线程上会让窗口短暂无响应，现在都放到后台线程执行。

### 新增

- **启动耗时与崩溃留痕**：新增 `~/.dsh-starter/logs/`——`startup.log` 记录启动各阶段与进程枚举的耗时，`panic.log` 记录崩溃位置与消息（release 版是 Windows GUI 程序，panic 默认没有任何输出）。白屏、崩溃这类问题下次可以直接看这两个文件定位。

## [0.1.3] - 2026-09-21

### 安全

- **伪 github 地址不再被代理**：只有真正的 github 域名才会走加速代理；像 `github.com.evil.test` 这类开头相同的相似主机不再被前缀匹配误判、悄悄丢给第三方代理（顺带修正了主机名大小写判定）。
- **外部输入统一转义 / 校验**：内置 Node 解压（PowerShell）、终端启动的 PATH 设置、GitHub ref / 包名等在落到
  shell 命令或 URL 之前会正确转义或校验。路径带引号（如用户名含 `'`）时不再把命令拼坏，也杜绝了借 URL 拼接
  访问到异常地址、把空参数拼丢导致命令行错位等问题。
- **git 克隆更防注入**：clone 安装 / 探测仓库时填的「分支 / 标签名」会先校验再拼给 git，以 `-` 开头的值
  （会被 git 当成命令行选项）直接拒绝；monorepo 子包路径里的 `..` 会被清洗，`link:` 目标始终落在克隆目录内，
  指不到仓库之外。
- **报错不再泄露凭据**：把带账号 / token 的私有 registry 地址写进错误提示或日志前会抹掉凭据（`https://user:tok@…`
  → `https://***@…`），避免 token 顺带显示在界面上。

### 修复

- **启动参数支持带空格的引号值**：在「默认启动参数 / 启动 args」里写 `--title "a b"` 这类含空格的参数时，此前会被按空格拆坏并把引号原样带进参数；现在按引号正确分组，`a b` 作为单个参数传给 dsh（单 / 双引号、转义空格都支持）。
- **停止 profile 不再可能误杀无关程序（Windows）**：靠端口反查实例归属时，若读不到目标进程的命令行（Windows 上此时只能凭映像名 `node.exe` 判断），此前会把任何恰好占着该端口的无关 Node 进程当成「本 profile 的实例」，停本 profile 时一并 taskkill 掉；现在拿不到命令行就拒绝归为本实例、只做「请手动处理」提示，界面仍会显示该端口为运行中。
- **独立进程启动登记失败不再谎报成功**：以「独立进程」方式启动时，若写入实例注册表失败（磁盘满、权限等），此前仍会提示「已启动」，但 macOS / Windows 上界面既看不到也停不掉这个实例；现在会立即终止刚拉起的 dsh 并如实报错。
- **实例列表轮询不再卡顿界面**：「运行中实例」每 3 秒刷新一次，此前这个枚举要在主线程里串行做进程存活校验、端口探测（每个连接最长等约 0.3 秒）、日志文件读取和 profile 扫描，端口多 / 有独立进程时整窗会短暂无响应；停止实例、停止 profile 同理。现在这几个操作都放到后台线程执行，界面不再被拖住。
- **关闭实例终端后不再后台空转读日志**：选中独立 / 外部进程实例、再收起「实例终端」抽屉后，此前仍每 1.5 秒 tail 一次它的日志文件、 indefinitely 在后台跑；现在只在抽屉打开时轮询。
- **安装内置 Node 时界面不再卡住**：下载完成后解压那几步（调用 tar / PowerShell 展开几十 MB 的归档，要跑好几秒）改到后台线程执行，解压期间窗口、托盘和其它点击不再短暂无响应。
- **添加凭据不再残留上一次输入**：打开「添加凭据」弹窗会先清空名称与值，避免上一次填的 token 悄悄留在表单里被再次带出。
- **插件终端「已省略 N 行」计数修正**：一次安装日志超出缓冲上限、旧行被裁掉时，此前提示一直显示 0；现在会如实累计被省略的行数。
- **软链接的 profile 不再被隐藏**：把 profile 目录 / 配置以符号链接放进 profiles 目录时，此前扫描既不当它是目录、也不当它是文件，直接从列表里消失（dsh 其实能透过软链运行）；现在解析软链目标后正常识别，指向不存在目标的坏软链仍安全跳过。
- **设置保存更抗损坏、token 权限收紧**：保存设置改为「先写临时文件、再原子替换」，写入过程中崩溃 / 断电不会再留下半截文件——此前损坏后下次启动会静默回到默认设置，把 GitHub token、镜像地址等一并覆盖掉；含 token 的设置文件权限收紧为仅本人可读写。
- **带构建号的版本号判定修正**：形如 `1.0.0+build.2` 的版本此前会被当成比 `1.0.0` 更新（误报有新版本、版本列表排错顺序）；现在按规范忽略 `+` 之后的构建元数据。
- **插件任务不再卡在进行中**：任务刚结束时刷新列表，更早的后端快照不会把已完成的任务退回「运行中」，避免转圈锁死、后续完成提示丢失；取消 / 清理遇到任务已消失也会自动与后端对齐。
- **版本「可升级」入口加在途守卫**：安装进行中时「可升级 →」链接一并禁用，不能连点重复触发安装。
- **配置文件不存在时可创建并保存**：dsh 全局配置在文件尚未生成时点「创建初始内容」，此前保存按钮仍置灰、无法落盘；现在创建后可正常保存。
- **自动更新不再误显示进度 / 卡死按钮**：deb / rpm 这类需要管理员授权的内置更新，后端本就不会静默自动安装，
  前端也不再自作主张把横幅顶成「下载安装中…」并禁用安装按钮——现在会正常显示「下载并安装（需管理员授权）」让你点。
- **更新安装中不再给「稍后」的假象**：一旦开始下载安装（成功后进程会自动重启、无法取消），横幅不再显示「稍后」，
  避免点了以为取消了、结果应用还是自己重启。
- **预发布版本渠道判定修正**：`-next` / `-canary` 等非 alpha/beta/rc 的预发布版本号不再被误标成「正式版」，
  归到预发布里，版本列表的筛选与标签都与实际一致。
- **右键托盘不再卡界面**：右键托盘刷新菜单同样改到后台线程执行（Windows 上枚举进程约需 1 秒），
  点了之后界面不会短暂失去响应。
- **打开配置详情不再卡界面**：查看某个 profile 的配置详情、切换插件包开关、创建「恢复模式」时，
  这些会调用 dsh / node 的重操作改到后台线程执行，主界面（窗口、托盘、其它点击）不再被短暂冻结。
- **配置保存不再误覆盖已有文件**：保存凭据、全局模型配置或插件开关时，若读取现有配置文件失败（权限 / 磁盘异常等，
  而非「文件不存在」），现在会直接报错并拒绝写入，不再把读取失败当成空文件、用新内容覆盖掉磁盘上已有的凭据 / 配置。
- **取消安装后不再卡住后续安装**：点「取消」杀掉带 `--foreground-scripts` 的安装时，后台收尾进程可能一直占着
  日志管道，导致安装任务久久不清、之后所有安装都被「已有安装任务正在进行」挡死；现在排空日志设了超时上限，
  取消后最多等几秒就放行。
- **设置页 GitHub 测速不再串结果**：打开设置读取缓存和手点「重新测速」并发时，较慢的旧请求不会覆盖较快新请求的结果，
  关闭抽屉后也不会再回写。
- **实例面板关闭后停止空转刷新**：右侧运行面板收起后不再每秒重渲染（运行时长只在打开时走动）。
- **本地克隆仓库删除防并发**：删除克隆仓库期间更新 / 删除 / link 按钮一并禁用并加在途守卫，避免旧的列表刷新把已删的行又填回来。
- **Windows 停止实例更安全**：结束进程 / 端口占用回退时，只有确认目标确为 node(dsh) 进程才动手，
  避免 PID 复用把无关程序误杀。
- **远端 refs 解析不再崩溃**：解析 git 引用列表时按字节处理，遇到含非 UTF-8 的引用名不再让整个查询崩溃。
- **插件页防串档**：快速切换 profile、或插件任务刷新间隙操作时，不再出现「把 A 的配置显示 / 保存进 B」的问题。
- **安装搜索防竞态**：连续搜索 / 连按回车不再让后返回的旧结果覆盖新结果（避免装成没搜过的包）；
  bundle 启用开关连点增加在途守卫。
- **切换 Node 来源失败会回滚**：保存失败时界面选项回退，不再与实际配置不一致。
- **scoped 包对账修复**：`@scope/包名@版本` 形式的插件不再被误解析成空包名，安装 / 卸载后的依赖对账恢复正常。
- **GitHub 加速不再干扰安装日志**：安装 / 流式任务的首行回显保持稳定，不受后台测速缓存影响。
- **启动器不再允许重复启动**：再次双击图标/快捷方式时，不再开出第二个主窗口、第二个托盘图标和两份后台轮询线程，
  而是把已有窗口唤回前台（隐藏到托盘的也会被唤出），新进程在创建窗口之前就退出；退出后单实例名正常释放。
- **连点「启动」不会起两个实例**：同一 profile 的启动请求在途时按钮转圈并禁用，避免后端唯一性守卫
  （依赖进程枚举，有扫描延迟）挡不住同一瞬间的两次请求。

### 文档

- 新增更新日志与 `docs/RELEASING.md` 发版手册：发版必须先在 CHANGELOG 写清本版本特性，CI 会校验并在缺失时失败。
- `AGENTS.md` 补齐开发规范（版本号三处同步、CHANGELOG 硬性要求、构建与验证、安全红线）。

## [0.1.2] - 2026-09-20

### 构建与发布

- 发布流水线换成 Tauri 官方模板（构建 + 发布两个作业），push 到 `main` 自动发版；版本号没变则自动跳过，不再白跑。
- 修复 Windows runner 上版本门禁步骤因默认使用 PowerShell 而失败的问题。

## [0.1.1] - 2026-09-20

### 新增

- deb / rpm 安装形态也支持应用内更新（仍只由用户点击触发，不会静默安装）。
- 更新包下载支持 GitHub 加速通道，`latest.json` 地址做规范化处理，避免重定向导致下载或验签失败。

### 构建与发布

- 三个平台产物**全部上传完成后**才把 Release 从草稿转正式，修掉多平台并发创建 Release 的竞态（此前先跑完的平台会发出版本不全的 Release）。
- 修复草稿 Release 按 tag 查不到的问题（草稿还没有 tag，改为按列表查 id）。

## [0.1.0] - 2026-09-20

首个公开版本。

### 新增

- **版本与安装**：从 npm registry 拉取 `@deepseek-ai/dsh` 的全部已发布版本（dist-tag、发布日期、体积，镜像可切换），
  一键安装 / 卸载 / 重装到启动器自己的数据目录；同时识别启动器安装、npm 全局安装、PATH 中的 `dsh` 三种来源并合并展示。
- **Node 运行时**：宿主机没有 Node 时可一键安装隔离的 Node LTS（用户级、无需 root、不污染系统），下载默认走镜像站。
- **三种启动方式**：内嵌子进程（日志实时回传「实例终端」）、独立进程（关闭启动器后 dsh 继续运行）、系统终端（交互式 TUI）。
- **Profile 管理**：扫描 `$DSH_HOME/profiles` 并选择启动；profile 卡片可折叠出 web 快捷配置与 patch 编辑；
  支持重命名、复制实例（自动错开 web 端口）、删除与回收站（可还原）、以及用 dsh 自带模板新建「恢复模式」。
- **每 profile 单实例**：内嵌与终端/外部启动的实例都会被识别，重复启动与端口占用都会给出可操作的错误提示，而不是让 dsh 秒退。
- **实例观测与停止**：独立进程 / 外部实例的发现、运行时长与日志查看、日志导出、统一的停止入口。
- **插件管理**：全链路走官方 `dsh plugin` 命令，内置终端流式输出、失败任务重试、GitHub 直装前核对构建产物；
  dsh 自带的 base / web-app / headless 禁止卸载与启停。
- **模型与凭据**：结构化编辑 `settings.yaml` 的 providers / agent-default-model 两节（支持 K/M 简写），凭据单独管理。
- **应用内更新**：检查 / 下载 / 验签 / 安装 + 重启，默认只在用户点击后执行；另有「自动下载安装」开关供愿意全自动的用户开启。
- **界面与托盘**：shadcn/ui + Tailwind CSS v4 全套组件、亮暗主题（默认跟随系统）、无边框自绘窗口外壳、
  侧栏可折叠的响应式布局；托盘常驻，左键切换窗口，右键查看各 profile 运行状态，关闭默认最小化到托盘。

### 修复

- dsh 启动约 10 秒后被强制停止（PDEATHSIG 误杀子进程）。
- 版本比较器语义反转导致「可升级」误判。
- profile 目录改为 `$DSH_HOME/profiles`（复数）并跳过 `node_modules`。
- 插件 ID 误报、插件页 profile 下拉被字母序抢占（应为 `web`）、正式版筛选把 RC 当成预发布。

[Unreleased]: https://github.com/Yinxe/dsh-starter/compare/v0.2.2...HEAD
[0.2.2]: https://github.com/Yinxe/dsh-starter/releases/tag/v0.2.2
[0.2.1]: https://github.com/Yinxe/dsh-starter/releases/tag/v0.2.1
[0.2.0]: https://github.com/Yinxe/dsh-starter/releases/tag/v0.2.0
[0.1.2]: https://github.com/Yinxe/dsh-starter/releases/tag/v0.1.2
[0.1.1]: https://github.com/Yinxe/dsh-starter/releases/tag/v0.1.1
[0.1.0]: https://github.com/Yinxe/dsh-starter/releases/tag/v0.1.0
