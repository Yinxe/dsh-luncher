# DSH Starter v0.1.1 发布说明

发布日期：2026-09-20
GitHub Release：https://github.com/Yinxe/dsh-starter/releases/tag/v0.1.1

> 本文件由 `npm run notes:archive` 从 CHANGELOG.md 生成，请勿手改。

### 新增

- deb / rpm 安装形态也支持应用内更新（仍只由用户点击触发，不会静默安装）。
- 更新包下载支持 GitHub 加速通道，`latest.json` 地址做规范化处理，避免重定向导致下载或验签失败。

### 构建与发布

- 三个平台产物**全部上传完成后**才把 Release 从草稿转正式，修掉多平台并发创建 Release 的竞态（此前先跑完的平台会发出版本不全的 Release）。
- 修复草稿 Release 按 tag 查不到的问题（草稿还没有 tag，改为按列表查 id）。

---

**安装包**：Windows `.exe` / `.msi`　·　Linux `.deb` / `.rpm` / `.AppImage`　·　macOS `universal .dmg`
**完整更新日志**：[CHANGELOG.md](https://github.com/Yinxe/dsh-starter/blob/main/CHANGELOG.md)
